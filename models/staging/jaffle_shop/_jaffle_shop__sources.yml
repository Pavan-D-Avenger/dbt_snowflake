version: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
      - name: orders
        freshness: 
          error_after:
            count: 2
            period: day
          warn_after:
            count: 1
            period: hour
        loaded_at_field: _etl_loaded_at
        
      
